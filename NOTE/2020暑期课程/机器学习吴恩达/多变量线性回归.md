# 多变量线性回归

## 假设函数 hypothesis function

$$
\begin{aligned}
    h_\theta(x) = \theta_0 + \theta_1 x_1 + \theta_2 x_2 + ...+ \theta_n x_n
\end{aligned}
$$

## gradient descent  

## Feature Scaling(特征缩放，考虑归一化？)

Make sure features are on a similar scale
